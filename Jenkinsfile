pipeline {
    agent any
    tools {nodejs "node"}
    stages{
      
        stage("pipeline")
        {
            
            steps{
                sh 'npm install'
                sh 'npm start
                
            }
        }
    }
}
